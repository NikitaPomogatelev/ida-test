{
  "name": "ida-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=master --cleanup"
  },
  "dependencies": {
    "core-js": "^3.8.3",
    "normalize.css": "^8.0.1",
    "nuxt": "^2.14.12"
  },
  "devDependencies": {
    "fibers": "^5.0.0",
    "node-sass": "^5.0.0",
    "push-dir": "^0.4.1",
    "sass": "^1.32.8",
    "sass-loader": "^10.1.1"
  }
}
